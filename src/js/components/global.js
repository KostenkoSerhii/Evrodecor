export function isResp991() {
  let windowWidth = $(document).width();

  if (windowWidth < 992) return true;
  return false;
}
export function isResp1169() {
  let windowWidth = $(document).width();

  if (windowWidth < 1170) return true;
  return false;
}
